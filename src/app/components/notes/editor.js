import React from 'react';
import TextArea from '../ui/textarea';

const Editor = () => {
    <Textarea onChange={(e) => this.changeNote} value={ this.state.note || '' } style={{ flex: 1 }}/>    
}

export default Editor;
